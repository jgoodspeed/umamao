%p= t('.greeting')

= simple_format(t('.message', :link => "stockcrunch.com"))

= link_to 'Check my email.', new_user_url(:affiliation_token => @affiliation_token)

%p= simple_format(t('notifier.global.signature'))

= simple_format(t('.goodbye'))
